# Theme name

One-line tagline (e.g., "Vaporwave on dark purple with chrome accents").

**Author:** Your name / handle / link
**License:** MIT (or whatever — drop a `LICENSE` file next to `theme.css` if different)
**Best paired with:** _UNIT3D theme name (id N)_ — works as Standalone or External
**File:** [`theme.css`](theme.css)
**Preview:** ![preview](preview.png)

## What it looks like

Two or three sentences describing the visual identity. What's the vibe, what's the signature touch?

## Usage

Paste this raw URL into **Settings → General → Style → Standalone CSS Stylesheet** (or **External CSS Stylesheet** to layer on top of a base theme):

```
https://git.coffee-n-cannabis.com/CnC/Community-Themes/raw/branch/main/themes/your-theme-name/theme.css
```

Save. Done.

## Tuning

The palette is in CSS custom properties at the top of `theme.css`. Override them from your own user-CSS to remix without forking. The main knobs:

- `--bg-base` — page background
- `--bg-elevated` — panels
- `--accent` — primary accent (links, focused borders, key buttons)
- (list any theme-specific knobs your CSS exposes)

## Known limitations / quirks

- (anything that doesn't look quite right; pages that aren't covered; etc.)

## Credit

- Original inspiration / source if applicable.
